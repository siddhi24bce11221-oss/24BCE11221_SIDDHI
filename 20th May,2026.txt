// constraints
Use vitcampus;
CREATE TABLE Persons (
    ID int NOT NULL,
    LastName varchar(255) unique not null,
    FirstName varchar(255) NOT NULL,
    Age int
);
desc persons;

insert into Persons values(101,'V','Jayanth',null);
select*from persons;
alter table Persons
add unique(Age);
alter table Persons
drop constraint Age;

drop table Persons;

 CREATE TABLE Persons (
    ID int NOT NULL,
    LastName varchar(255) NOT NULL,
    FirstName varchar(255),
    Age int,
    CHECK (Age>=18)
);
drop table persons;
CREATE TABLE Persons (
  
    Age int not null,
    CHECK (Age>=18 and age <=23)
);

insert into persons values (21);

ALTER TABLE Persons	
ADD CHECK (Age>=18);

alter table persons
add primary key (ID);

alter table Persons
drop Primary Key;

drop table persons;
drop table persons1;
CREATE TABLE Persons1 (
    ID int NOT NULL,
    LastName varchar(255) NOT NULL,
    FirstName varchar(255),
    Age int,
    City varchar(255) default 'Coimbatore'
);
desc persons1;
drop table products;
create table products(
pid int primary key,
pname varchar(100) not null,
pref int not null unique
);

desc products;
insert into products values (102,"furniture",19930);
select * from products;
insert into products values(101,'electronics',19929);
select*from products;

select * from persons;
 drop table category;
 create table category(
 cid int primary key,
 cname varchar(100) not null
 );
 insert into category values (101,'electronics'),(102,'Home Appliances');
 select* from category;
 drop table products;
 CREATE TABLE products (
    pid int primary key,
    pname varchar(100) NOT NULL,
    cid int, 
    FOREIGN KEY (cid) REFERENCES category(cid)
);
desc products;
insert into products values(501,'HP Laptop',101);
DELETE FROM products WHERE cid = 101;
DELETE FROM category WHERE cid = 101;


INSERT INTO category VALUES (105, 'Electronics');

INSERT INTO products (pid, pname, cid) 
VALUES (504, 'Power Bank Samsung', 105);
insert into products values (504,'Power Bank Samsung',64-bit key);

select * from products;